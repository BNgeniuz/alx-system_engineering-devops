#!/usr/bin/env bash
# Display Time - Hour, Minutes
# You must use the while loop (for and until are forbidden
hour=0

while [ $hour -le 12 ]
do
  echo "Hour: $hour"
  minute=1
  while [ $minute -le 59 ]
  do
    echo "$minute"
    (( minute++ ))
  done
  (( hour++ ))
done
